import 'package:flutter/material.dart';

class AccoodationScreen extends StatelessWidget {
  static const routeName = '/accomodation-screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Icon(
          Icons.adb,
          size: 100,
        ),
      ),
    );
  }
}
